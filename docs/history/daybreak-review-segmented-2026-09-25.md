[BLOCKER] Repository inspection failed: every filesystem/command attempt—including the required `git log --oneline -12` and `git diff 98dd25a..HEAD`—was rejected by the sandbox with `error building bubblewrap command: app-server socket directory has an unsupported host mount at /mnt/wslg/distro`. Consequently, file:line evidence could not be collected without fabrication.

1. **NOT VERIFIED** — inventory ownership validation.
2. **NOT VERIFIED** — pin/effort conflict rejection.
3. **NOT VERIFIED** — per-effort derived resources and snapshots.
4. **NOT VERIFIED** — bounded retirement history and observation pruning.
5. **NOT VERIFIED** — concurrency state across rebuilds.

SEGMENT-VERDICT: FAIL
[BLOCKER] Controlled homes are bypassable through config injection and symlinks. `_config_is_contained()` only checks the notice prefix and regex-matched project tables; a notice followed by `[mcp_servers.evil]` passes unchanged, allowing configuration-defined processes to escape the turn sandbox and potentially read another source’s credentials (`scarcity_router/worker_codex_adapter.py:550-578`). Additionally, tree creation follows existing symlinks, while validation uses `os.stat()`/`is_file()`, which also follow them; making one source directory point at another collapses both `CODEX_HOME`s (`scarcity_router/worker_identity_store.py:562-575`, `scarcity_router/worker_codex_adapter.py:614-632`).

[BLOCKER] `SLUG_MAX_LENGTH` omits the mandatory effort suffix. The contract permits a 20-character source and 40-character classified slug, but `<source>:<slug>:medium` is 68 characters and fails the 64-character safe-ID validator (`scarcity_router/model_inventory.py:68-97`). Adoption mutates `routable_now` and `state.adopted` before constructing that invalid ID, leaving poisoned partial state when construction raises (`scarcity_router/execution_sources.py:253-270`). The worker’s preflight checks only `source+slug`, not each effort-qualified ID (`scarcity_router/worker_codex_adapter.py:1637-1642`).

[BLOCKER] Inventory collection limits are enforced after eagerly materializing attacker-sized lists. All effort objects, model objects, and source objects are constructed before the respective 8/64-element checks run (`scarcity_router/model_inventory.py:239-252`, `scarcity_router/model_inventory.py:312-324`, `scarcity_router/model_inventory.py:364-381`, `scarcity_router/model_inventory.py:455-465`). An authenticated worker can therefore repeatedly submit a 16 MiB frame and force large Python object/CPU amplification before rejection (`scarcity_router/worker_protocol.py:88-94`).

[BLOCKER] Duplicate sources are rejected only within one `ModelInventoryReport`, not across the up-to-eight inventory documents in one state report (`scarcity_router/model_inventory.py:415-420`, `scarcity_router/worker_protocol.py:920-939`). The endpoint then applies those documents sequentially (`scarcity_router/worker_endpoint.py:561-603`). Three authenticated empty inventories for the same source in one frame increment the miss counter three times and immediately retire its resources (`scarcity_router/execution_sources.py:277-293`), defeating report-level duplicate rejection and observation cadence.

[BLOCKER] Worker observations can smuggle arbitrary quota-pool membership. `ResourceIdentity.from_dict()` accepts `quota_pool_ids`, but observation matching deliberately excludes them (`scarcity_router/resource_state.py:682-704`, `scarcity_router/resource_state.py:1142-1155`). The uncanonicalized snapshot is then stored and serialized inside the registry entry (`scarcity_router/resource_state.py:1596-1612`, `scarcity_router/resource_state.py:1292-1294`). Thus a resource registered in account A’s pool can expose an observation claiming account B’s pool. Authoritative top-level pool groups still use registration identity, but the resulting document is contradictory and unsafe for consumers.

[BLOCKER] The inventory is not credential/account-metadata-free by construction. The module claims it has no free-text carrier (`scarcity_router/model_inventory.py:15-18`), yet `display_name` accepts and serializes any 128-character string (`scarcity_router/model_inventory.py:176-215`, `scarcity_router/model_inventory.py:230-237`). A malformed worker can place an email, account ID, or token fragment there and have the server accept and retain it. The current official producer omits this field, but the wire contract itself does not enforce the stated invariant.

[NOTE] Cross-worker source spoofing is closed: the endpoint matches `inventory.worker_id` to the authenticated worker and verifies every reported source is configured for that worker (`scarcity_router/worker_endpoint.py:564-582`).

[NOTE] The routable colon-confusion path is currently closed. Server source IDs explicitly reject `:`, and reviewed routable track patterns cannot match colon-bearing slugs (`scarcity_router/server_config.py:344-357`, `model-tracks.json:7-11`, `model-tracks.json:33-37`, `model-tracks.json:59-63`). The generic inventory/worker validators still allow colons, so the component grammar should be made uniform, but such values remain unclassified or unbound.

[NOTE] Strict JSON duplicate-key rejection and exact object shapes are present (`scarcity_router/model_inventory.py:137-163`, `scarcity_router/model_inventory.py:347-363`). They do not close the eager-list or cross-document duplicate findings above.

SEGMENT-VERDICT: FAIL
[BLOCKER] **Pinned effort is not enforced end-to-end, so `gpt-6-sol/high` can execute another/default effort while audit records `high`.** The coordinator rejects only an explicitly conflicting effort; when the request omits `reasoning_effort`, it forwards `None` rather than the selected variant (`scarcity_router/gateway_coordinator.py:696-720`). In legacy Codex mode the worker checks only provider/model, never binds the selected variant (`scarcity_router/worker_codex_adapter.py:1749-1752,1779-1790`). `verify_model_and_effort` accepts `None`, and the turn omits the effort entirely, allowing the runtime default (`scarcity_router/worker_codex_adapter.py:1256-1265,1860-1864,2068-2069`). Additionally, a compromised coordinator can send a source resource bound to `high` with `call.model.variant=high` but `reasoning_effort=low`: the source branch only fills an absent effort and never rejects a present mismatch (`scarcity_router/worker_codex_adapter.py:1753-1778`). If `low` is runtime-supported, verification passes and executes it. Model substitution itself is closed by the model equality check and explicit `model` turn parameter (`scarcity_router/worker_codex_adapter.py:1779-1790,2056-2069`). Audit does not detect the effort substitution: it sets `executed_target = selected_target` before dispatch and persists that value (`scarcity_router/gateway_coordinator.py:711-724,793-805`).

[BLOCKER] **Provider-auth loss can be reported together with a stale healthy snapshot, undoing the server’s purge and leaving the source apparently routable.** `_send_state_report` collects resource snapshots first, then refreshes source inventory (`scarcity_router/worker_client.py:872-900,905-940`). Thus a due refresh can produce `auth_required` with no models while the same report already contains snapshots derived from the previous authenticated inventory. The server applies the unauthenticated inventory and purges observations, but subsequently applies and caches the stale snapshots from that report (`scarcity_router/control_api.py:480-504,506-518`; `scarcity_router/worker_endpoint.py:593-659`). This is exploitable through ordinary timing: discovery refreshes after 300 seconds while derived-resource freshness is 900 seconds, so the old healthy snapshot can remain fresh for roughly another 600 seconds (`scarcity_router/worker_codex_adapter.py:126-129`; `scarcity_router/execution_sources.py:116-119,161-168`). Execution still rechecks provider auth and should fail, but routing/auth-state honesty is broken.

[BLOCKER] **The controlled Codex home does not enforce its promised “no MCP/plugins/apps” boundary.** `_config_is_contained` merely checks a notice prefix and regex-matched project headings; a config consisting of the notice followed by `[mcp_servers.attacker]` and a command contains no rejected project match and is accepted (`scarcity_router/worker_codex_adapter.py:515-520,568-578`). Project containment is also a lexical `startswith`, accepting prefix tricks rather than resolved descendants (`scarcity_router/worker_codex_adapter.py:574-577`). The rewrite path preserves arbitrary top-level content preceding project tables (`scarcity_router/worker_codex_adapter.py:580-612`). Subsequent app-server sessions use this home (`scarcity_router/worker_codex_adapter.py:1839-1844`), so the isolation policy does not actually prevent injected runtime configuration.

[NOTE] **Worker pairing and provider authentication are otherwise kept distinct.** Provider auth has a strict four-value vocabulary and rejects unknown values (`scarcity_router/model_inventory.py:51-62,260-304`); the UI explicitly explains that source auth is provider sign-in and worker pairing is separate (`scarcity_router/server_ui.py:1108-1112`). Pairing alone therefore does not become `authenticated`. However, the source view’s `connected` flag means only “an inventory has ever been received,” not that the worker is currently connected, so it can remain stale after disconnect (`scarcity_router/execution_sources.py:429-439`; `scarcity_router/control_api.py:1281-1295`).

[NOTE] **Cross-worker inventory spoofing and adapter escalation are closed, but report application is not fully transactional.** The endpoint checks both report worker identity and every source against the authenticated worker before applying any inventory (`scarcity_router/worker_endpoint.py:527-592`); resource ownership is reread from local configuration (`scarcity_router/worker_endpoint.py:612-633,840-874`); and the worker executes only locally allowlisted adapters (`scarcity_router/worker_client.py:737-755`). However, authorized inventories are applied before foreign resource snapshots are validated, so a report can return “rejected” after already adopting or retiring the sender’s own source state (`scarcity_router/worker_endpoint.py:593-633`). This does not grant cross-worker authority, but violates the stated whole-report atomicity.

[NOTE] **Retirement does not rewrite audit history.** Retirement mutates only source-registry operational state (`scarcity_router/execution_sources.py:273-294`), while audit records contain copied target identities and are appended (`scarcity_router/gateway_coordinator.py:785-805,856-862`). No retirement-to-audit mutation path was found. The effort bypass above nevertheless makes newly written audit history inaccurate.

[NOTE] **TLS and SSH-safe login paths are largely closed as claimed.** Worker TLS uses the default verifying context with hostname checking, and plaintext origins are restricted to the explicit loopback vocabulary (`scarcity_router/worker_client.py:124-150,201-235`). Non-loopback worker listeners require TLS (`scarcity_router/worker_endpoint.py:1004-1024`). The exception is lexical: `localhost` is trusted without verifying the resolved/bound address is loopback, making hostile local name-resolution configuration a hardening concern. Device login probes for `--device-auth`, invokes only that mode, overrides `CODEX_HOME` to the source-specific home, and has no browser/localhost fallback (`scarcity_router/worker_codex_adapter.py:2478-2549`). No legacy-home lookup or credential-copying path was found.

SEGMENT-VERDICT: FAIL
